version: '3.8'

services:
  web:
    build: .
    ports:
      - "80:80"
    restart: always
    environment:
      - NODE_ENV=production
    networks:
      - app-network

  # backend:
  #   image: my-backend-image
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     - DB_HOST=db
  #   networks:
  #     - app-network
  #   depends_on:
  #     - db

  # db:
  #   image: postgres:13
  #   environment:
  #     - POSTGRES_USER=user
  #     - POSTGRES_PASSWORD=password
  #     - POSTGRES_DB=mydb
  #   volumes:
  #     - db-data:/var/lib/postgresql/data
  #   networks:
  #     - app-network

networks:
  app-network:
    driver: bridge

volumes:
  db-data:
